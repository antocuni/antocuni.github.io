<!-- -*- mode: markdown -*- -->

<style>

  :root {
    --r-main-font-size: 40px;  /* the default is 40px */
  }

  .reveal h1, .reveal h2, .reveal h3, .reveal h4, .reveal h5 {
    text-transform: none;
  }

  .reveal h1, .reveal h2, .reveal h3, .reveal h4, .reveal h5 {
    text-transform: none;
  }

  /*
   * Code blocks default to 75% width, larger font, 600px max-height
   * (scrolls beyond that). Override per-slide with a directive right
   * after the slide title:
   *
   *   ## My slide
   *
   *   <!-- .slide: style="--code-max-width: 700px;
   *                       --code-font-size: 1.2em;
   *                       --code-max-height: 950px;" -->
   *
   *   ```python
   *   ...
   *   ```
   *
   * The same variables also affect ```autorun``` terminal blocks.
   */
  .reveal pre {
    width: var(--code-max-width, 75%);
    max-width: 100%;
    font-size: var(--code-font-size, 1.3em);
  }

  .reveal pre code {
    padding: 5px;
    overflow: auto;
    max-height: var(--code-max-height, 600px);
    word-wrap: normal;
  }

  .big {
    font-size: 2.5em;
  }

  .small {
    font-size: 0.8em;
  }

  .main-size {
    font-size: var(--r-main-font-size);
  }
</style>



<!-- you can also use this comment to set a custom html title (remove the xxx) -->
<!-- xxx-title: ... -->

# Title title title

XXX update link!

<qr-code data="https://antocuni.eu/talk/2025/...." format="svg" modulesize="8" margin="4"></qr-code>

<span class="small">https://antocuni.eu/talk/2025/....</span>


---

## Slide 1

---

## Slide 2

---

## Big code block

<!-- .slide: style="--code-font-size: 0.8em; --code-max-height: 950px;" -->

```python
# Use --code-max-height to let a code block grow to fill the slide.
# Use --code-font-size to shrink the font so more lines fit.
def main() -> None:
    print("hello")
```
