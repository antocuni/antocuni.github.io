
@blue
def make_adder(y: dynamic):
    T = type(y)

    def add(x: T) -> T:
        return x + y

    return add

add5 = make_adder(5)
add_world = make_adder(" world")

def main() -> None:
    print(add5(2))
    print(add_world("hello"))
